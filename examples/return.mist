fn f(x: int) -> int
    ens result == if x > 0 { x } else { -x }
{
    if x > 0 {
        return x;
    };

    -x
}

struct P { a: int }

fn g(x: &mut P) -> int {
    return x.a
}

fn q() {
    let x = return;
}
