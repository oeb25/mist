// TODO: votes should be Seq[int]
fn count_votes(votes: Seq, candidate: int, low: int, high: int) -> int
  requires 0 <= low, low <= high, high <= votes
{
  if low == high {
    0
  } else {
    count_votes(votes, candidate, low, high - 1) +
      if votes[high - 1] == candidate {
        1
      } else {
        0
      }
  }
}
